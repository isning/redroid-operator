# CRD files — generated automatically

The YAML files in this directory are generated by `controller-gen` and kept in
sync from `config/crd/bases/` by the Makefile target:

```
make helm-crds
```

**Do not edit these files by hand.**  
To regenerate after changing the API types:

```
make manifests helm-crds
```
